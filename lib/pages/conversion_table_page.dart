import 'package:flutter/material.dart';

class ConversionTablePage extends StatelessWidget {
  const ConversionTablePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tabla de Conversión'),
    );
  }
} 